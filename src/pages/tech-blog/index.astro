---
import { getCollection } from 'astro:content';
import BaseLayout from '../../layouts/BaseLayout.astro';
import PostListItem from '../../components/PostListItem.astro';

const posts = (await getCollection('tech-blog'))
  .filter(p => !p.data.draft)
  .sort((a, b) => b.data.date.valueOf() - a.data.date.valueOf());
---

<BaseLayout title="Notes" description="Things I’m learning and trying to understand">
  <div class="page-header">
    <h1>Notes</h1>
    <p>正在学习的 | Things I’m learning and trying to understand</p>
  </div>

  {posts.length === 0 ? (
    <p style="color: var(--color-text-muted);">还没有文章，敬请期待。</p>
  ) : (
    <ul class="post-list">
      {posts.map(p => (
        <PostListItem
          title={p.data.title}
          date={p.data.date}
          slug={p.slug}
          section="tech-blog"
          description={p.data.description}
        />
      ))}
    </ul>
  )}
</BaseLayout>
